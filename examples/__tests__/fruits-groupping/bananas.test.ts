describe('fruits', () => {
  describe('bananas', () => {
    it('should be long', async () => {
      expect(1).toBe(1);
    });

    it('should be yellow or green or black', async () => {
      expect(1).toBe(1);
    });
  });
});
