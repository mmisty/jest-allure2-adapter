describe('fruits.pear.small.long', () => {
  describe('long2.long3.long4.long5', () => {
    it('should have strange form', async () => {
      expect(1).toBe(1);
    });

    it('should be yellow or green or brown', async () => {
      expect(1).toBe(1);
    });
  });

  it('should be thing', async () => {
    expect(1).toBe(1);
  });
});
