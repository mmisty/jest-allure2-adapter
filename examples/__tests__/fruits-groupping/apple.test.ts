
describe('fruits', () => {
describe('apples', () => {

  it('should have stick', async () => {
    expect(1).toBe(1);
  });

  it('should wight ~100-400 gramms', async () => {
    expect(1).toBe(1);
  });

});
});
