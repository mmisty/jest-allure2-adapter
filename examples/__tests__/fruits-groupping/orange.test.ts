
describe('fruits', () => {
describe('oranges', () => {

  it('should be orange', async () => {
    expect(1).toBe(1);
  });

  it('should be round', async () => {
    expect(1).toBe(1);
  });

  it('should wight ~500 gramms', async () => {
    expect(1).toBe(1);
  });

});
});
